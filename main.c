#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main()
{
    printf("HI\n");
//1 pointer
    /*int n[10],a=0,b=0,c;
    int *ptrn;

    for(c=0;c<10;c++)
    {
        printf("Input:");
        scanf("%d",&n[c]);
    }
    ptrn =&n;

    for(c=0;c<10;c++)
    {
        if(*(ptrn+c)%2==0)
        {
            b=b+(*(ptrn+c));
        }
    }
    printf("%d",b);*/

//2


//3
    /*char n[1000];
    char *pcha;
    int a,b,i;

    printf("Input:");
    scanf("%s",n);

    a=strlen(n);
    pcha =&n;

    for(i=0;i<a;i++)
    {
        if(i%2==0)
        {
            b=toupper(*(pcha+i));
            printf("%c",b);
        }

        else
        {
            b=tolower(*(pcha+i));
            printf("%c",b);
        }
    }*/


    return 0;
}
